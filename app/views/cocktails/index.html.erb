
<div class="container">
  <div class="row"></div>
  <h1>available Cocktails</h1>

  <% @cocktails.each do |cocktail| %>
  <h2><%= link_to cocktail.name, cocktail_path(cocktail.id) %></h2>
  <% end %>

<hr>
      <%= button_to 'Add new cocktail', new_cocktail_path, method: :get, class: 'btn btn-primary' %>
    </div>
</div>
